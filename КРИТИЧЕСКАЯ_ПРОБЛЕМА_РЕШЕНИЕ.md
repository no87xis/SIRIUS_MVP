# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА: Префикс "qс" в терминале

## ❌ ПРОБЛЕМА
**ВСЕ команды в терминале получают префикс "qс" (где вторая буква - кириллическая "с")**

**Примеры:**
- `qсpython --version` вместо `python --version`
- `qсecho ok` вместо `echo ok`
- `qсcmd /c` вместо `cmd /c`
- `qсpowershell` вместо `powershell`

## 🔍 ДИАГНОСТИКА
Проблема настолько серьезная, что даже команды исправления получают префикс "qс" и не выполняются.

## 🔧 РЕШЕНИЕ

### 🥇 СПОСОБ 1: Перезапуск терминала (Попробуйте сначала)
1. **Закройте текущий терминал**
2. **Откройте новый терминал**
3. **Проверьте команды**: `echo ok`, `python --version`

### 🥈 СПОСОБ 2: Перезагрузка компьютера
1. **Перезагрузите компьютер**
2. **Откройте новый терминал**
3. **Проверьте команды**: `echo ok`, `python --version`

### 🥉 СПОСОБ 3: Использование cmd вместо PowerShell
1. **Нажмите Win+R**
2. **Введите**: `cmd`
3. **Нажмите Enter**
4. **Проверьте команды**: `echo ok`, `python --version`

### 🛡️ СПОСОБ 4: Использование VS Code терминала
1. **Откройте VS Code**
2. **Откройте терминал** (Ctrl+`)
3. **Проверьте команды**: `echo ok`, `python --version`

### 🔧 СПОСОБ 5: Ручное исправление через проводник
1. **Откройте проводник**
2. **Перейдите в папку проекта**
3. **Дважды щелкните** на `START_SERVER_NOW.bat`
4. **Если не работает, попробуйте** `QUICK_START.bat`

## 🎯 АЛЬТЕРНАТИВНЫЙ ЗАПУСК СЕРВЕРА

### Вариант A: Через проводник
1. **Откройте папку проекта в проводнике**
2. **Дважды щелкните** на `START_SERVER_NOW.bat`
3. **Если не работает, попробуйте** `QUICK_START.bat`

### Вариант B: Через VS Code
1. **Откройте проект в VS Code**
2. **Откройте терминал** (Ctrl+`)
3. **Выполните команду**:
   ```bash
   uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
   ```

### Вариант C: Через cmd
1. **Нажмите Win+R**
2. **Введите**: `cmd`
3. **Перейдите в папку проекта**:
   ```cmd
   cd "D:\!РАЗРАБОТКА ПО\Sirius_sklad_new-master"
   ```
4. **Запустите сервер**:
   ```cmd
   uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
   ```

## 🌐 ПОСЛЕ ЗАПУСКА СЕРВЕРА

Откройте в браузере:
- **🏠 Главная**: http://127.0.0.1:8000
- **🛒 Магазин**: http://127.0.0.1:8000/shop
- **👤 Админ**: http://127.0.0.1:8000/admin
- **📚 API Docs**: http://127.0.0.1:8000/docs
- **🏥 Health**: http://127.0.0.1:8000/health

## 🆘 ЕСЛИ НИЧЕГО НЕ ПОМОГАЕТ

### Последний способ:
1. **Перезагрузите компьютер**
2. **Откройте cmd** (Win+R → cmd)
3. **Перейдите в папку проекта**
4. **Запустите**:
   ```cmd
   python -m pip install -r requirements.txt
   python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
   ```

## 📁 ГОТОВЫЕ ФАЙЛЫ ДЛЯ ЗАПУСКА

В папке проекта есть готовые файлы:
- `START_SERVER_NOW.bat` - Основной запуск
- `QUICK_START.bat` - Быстрый запуск
- `start_local.bat` - Альтернативный запуск
- `fix_terminal_direct.py` - Исправление (если терминал заработает)

## 🎉 ИТОГ

**Проблема**: Префикс "qс" в терминале
**Решение**: Используйте альтернативные способы запуска
**Результат**: Сервер запустится и будет работать

---

**🚀 Попробуйте перезапустить терминал или использовать cmd/VS Code!**



